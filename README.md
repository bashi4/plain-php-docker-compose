# plain-php-docker-compose
Runnnig PHP, Nginx, and MySQL with Docker and Docker Compose

Open a terminal and `cd` to the folder in which docker-compose.yml is saved and run:
```
docker-compose up
````
